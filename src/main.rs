fn main() {
    let opcode: u16;
    let memory: [char; 4096];
    let v: [char; 16];
    let i: u16;
    let pc: u16;
    let gfx: [char; 64 * 32];
    let delay_timer: char;
    let sound_timer: char;
    let stack: u16;
    let sp: u16;
    let key: [char; 16];
}
